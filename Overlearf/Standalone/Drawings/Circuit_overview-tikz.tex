\documentclass[11pt,border=0mm,tikz]{standalone}
\input{Setup/StandalonePreamble}

\providecommand{\FigScale}{1}

\usepackage{siunitx}
\usepackage{tikz}
\usetikzlibrary{decorations.pathmorphing,patterns}
\usetikzlibrary{shapes.geometric, arrows.meta}
\usetikzlibrary{positioning}

\usepackage{amsmath}
\usepackage{upgreek}
\usepackage{svg}

\usetikzlibrary{3d}
\usetikzlibrary{calc}

\tikzset{
box/.style={
	rectangle,
	rounded corners=4pt,
	minimum width=3cm,
	minimum height=1cm,
	text centered,
	draw=black!70,
	line width=0.95pt,
	fill=gray!5,
	inner sep=4pt
},
circlebox/.style={
	circle,
	minimum size=1.5cm,
	text centered,
	draw=black!70,
	line width=0.95pt,
	fill=gray!5,
	inner sep=1pt
},
arrow/.style={
	thick,
	->,
	>=Stealth,
	draw=black!70,
	line width=1pt,
	line cap=round,
	line join=round,
	shorten <=1pt,
	shorten >=1pt
}
}

\begin{document}

\begin{tikzpicture}[scale=\FigScale, node distance=2cm]

\node (Battery) [box] {Battery};
\node (LDO) [box, below of=Battery] {LDO};
\node (MCU) [box, below of=LDO] {MCU};
% \node (Buck) [box, right of=MCU, xshift=3cm] {Buck};
% \node (Reset) [box, left of=MCU, above of=MCU, xshift=-3cm, yshift=-0.75cm] {Reset};
\node (RF) [box, left of=MCU, xshift=-3cm] {RF};
\node (PMOS) [box, right of=MCU, xshift=3cm] {PMOS};

\node (Sensor) [box, below of=MCU] {Sensor};

\draw [arrow] (Battery.south) -- (LDO.north);
\draw [arrow] (LDO.south) -- (MCU.north);

% \draw [arrow] ($(MCU.east)!0.5!(MCU.north east)$) -- node[midway, above] {\large $\SI{3}{\volt}$} ($(Buck.west)!0.5!(Buck.north west)$);
% \draw [arrow] ($(Buck.west)!0.5!(Buck.south west)$) -- node[midway, below] {\large $\SI{1.8}{\volt}$} ($(MCU.east)!0.5!(MCU.south east)$);
% \draw [arrow] ($(Reset.east)!0.5!(Reset.south east)$) -- ($(MCU.west)!0.5!(MCU.north west)$);
% \draw [arrow] ($(MCU.west)!0.3!(MCU.south west)$) -- node[midway, above, rotate=20] {\large $\SI{1.8}{\volt}$} ($(RF.east)!0.7!(RF.north east)$);

\draw [arrow] (RF.east) -- (MCU.west);
\draw [arrow] (MCU.west) -- (RF.east);

\draw [arrow] (MCU.east) -- node[midway, above] {\large GPIO} (PMOS.west);
\draw [arrow] (LDO.east) to[out=0, in=90] (PMOS.north);
\draw [arrow] (PMOS.south) to[out=-90, in=0] (Sensor.east);

\draw [arrow] (Sensor.north) -- node[midway, right] {\large ADC} (MCU.south);

\end{tikzpicture}

\end{document}
