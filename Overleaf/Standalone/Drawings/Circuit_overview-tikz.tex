\documentclass[border=0mm,tikz]{standalone}
\input{Setup/StandalonePreamble}

\usepackage{siunitx}
\usepackage{tikz}
\usetikzlibrary{decorations.pathmorphing,patterns}
\usetikzlibrary{shapes.geometric, arrows.meta}
\usetikzlibrary{positioning}

\usepackage{amsmath}
\usepackage{upgreek}
\usepackage{svg}

\usetikzlibrary{3d}
\usetikzlibrary{calc}

\tikzset{
box/.style={
	rectangle,
	rounded corners=4pt,
	minimum width=3cm,
	minimum height=1cm,
	text centered,
	draw=black!70,
	line width=0.95pt,
	fill=gray!5,
	inner sep=4pt
},
circlebox/.style={
	circle,
	minimum size=1.5cm,
	text centered,
	draw=black!70,
	line width=0.95pt,
	fill=gray!5,
	inner sep=1pt
},
arrow/.style={
	thick,
	->,
	>=Stealth,
	draw=black!70,
	line width=1pt,
	line cap=round,
	line join=round,
	shorten <=1pt,
	shorten >=1pt
}
}

\begin{document}

\begin{tikzpicture}[node distance=2cm]

\node (Battery) [box] {Battery};
\node (LDO) [box, below of=Battery] {LDO};
\node (MCU) [box, below of=LDO] {MCU};
% \node (Buck) [box, right of=MCU, xshift=3cm] {Buck};
% \node (Reset) [box, left of=MCU, above of=MCU, xshift=-3cm, yshift=-0.75cm] {Reset};
\node (RF) [box, left of=MCU, xshift=-3cm] {RF};
\node (PMOS) [box, below of=MCU] {PMOS};

\node (Sensor) [box, below of=PMOS] {Sensor};

\draw [arrow] (Battery.south) -- node[midway, right] {\large $\SI{3.6}{\volt}$} (LDO.north);
\draw [arrow] (LDO.south) -- node[midway, right] {\large $\SI{3}{\volt}$} (MCU.north);
% \draw [arrow] ($(MCU.east)!0.5!(MCU.north east)$) -- node[midway, above] {\large $\SI{3}{\volt}$} ($(Buck.west)!0.5!(Buck.north west)$);
% \draw [arrow] ($(Buck.west)!0.5!(Buck.south west)$) -- node[midway, below] {\large $\SI{1.8}{\volt}$} ($(MCU.east)!0.5!(MCU.south east)$);
% \draw [arrow] ($(Reset.east)!0.5!(Reset.south east)$) -- ($(MCU.west)!0.5!(MCU.north west)$);
% \draw [arrow] ($(MCU.west)!0.3!(MCU.south west)$) -- node[midway, above, rotate=20] {\large $\SI{1.8}{\volt}$} ($(RF.east)!0.7!(RF.north east)$);
\draw [arrow] (RF.east) -- (MCU.west);
\draw [arrow] (MCU.west) -- (RF.east);
\draw [arrow] (MCU.south) -- node[midway, right] {\large GPIO} (PMOS.north);
\draw [arrow] (PMOS.south) -- node[midway, right] {\large $\SI{3}{\volt}$} (Sensor.north);
\draw [arrow] ($(Sensor.north east)!0.2!(Sensor.north)$) to[out=35, in=-35] node[midway, right] {\large ADC} ($(MCU.south east)!0.2!(MCU.south)$);

\end{tikzpicture}

\end{document}